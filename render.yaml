services:
  - type: web
    name: eyeviacontroller-api
    env: python
    plan: free
    region: oregon
    branch: main
    buildCommand: pip install -r EyeViaController/requirements.txt
    startCommand: uvicorn EyeViaController.app:app --host 0.0.0.0 --port $PORT
